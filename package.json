{
  "dependencies": {
    "compression": "^1.7.4",
    "heapdump": "^0.3.15",
    "sharp": "^0.33.4"
  },
  "scripts": {
    "build": "npx concurrently \"npm run build --prefix frontend\" \"echo 'No build step for backend'\"",
    "start": "npm start --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  }
}
