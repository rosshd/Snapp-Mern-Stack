{
  "dependencies": {
    "compression": "^1.7.4",
    "heapdump": "^0.3.15",
    "sharp": "^0.33.4"
  },
  "scripts": {
  "build": "npx concurrently \"npm run build --prefix frontend\" \"npm run build --prefix backend\"",
  "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\"",
  "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
},

  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
